# 의사결정 기록

> 갈림길에서 *왜 X 대신 Y를 했는지*, *왜 지금은 Z를 안 했는지* 같이
> **코드만 봐서는 안 보이는 선택의 맥락**을 기록한다.
>
> TROUBLESHOOTING.md와의 차이: TROUBLESHOOTING은 *문제 → 해결*.
> 여기는 *갈림길 → 선택 + 이유*. 둘 다 아니면 보통 어디에도 기록 불필요.
>
> **시간 역순** — 최신이 위.

---

## 2026-05-17 — TDD 엄밀성 일시 완화 (mutation 검증 생략)

**상황**: §6.3 FieldGenerator 작성 중. 9개 테스트 중 마지막 3개(`count=0`, `count=1`, 같은 시드 재현성)가 RED→GREEN 전환 없이 추가 즉시 통과. 엄격한 TDD라면 (a) 순서를 바꿔 재현성 테스트를 시드 기반 RNG 도입 *전*에 쓰거나, (b) 통과 후 일시 mutation(예: `mulberry32(seed)` → `mulberry32(Date.now())`)으로 테스트가 실제로 RED 되는지 확인해야 함.

**결정**: 둘 다 생략하고 그대로 진행.

**이유**: Phase 1의 우선순위는 *"찾는 행위가 재밌는가?"* 에 빠르게 답하는 것 (PROJECT.md §5). TDD 엄밀성보다 진행 속도에 비중. 3개 테스트는 regression 가드로서는 여전히 가치 있음 — 미래에 `Math.random()` 끼워넣기나 count=0 분기 누락이 들어오면 RED로 잡아줌.

**트레이드오프**: 그 3개 테스트가 *진짜로 의도한 속성을 검증하는지* 증명되지 않음. 미묘한 assertion 오류가 있어도 못 잡았을 가능성. 단, 현재 구현이 단순해서 위험 낮다고 판단.

**재검토 시점**: Phase 2 이후 코어 로직(점수/컬렉션/영속성)이 들어오면 잘못된 테스트 비용이 급격히 커짐. 그 시점에 TDD 엄밀성 한 단계 올릴지(예: mutation 검증 의무화) 다시 결정.

**관련 커밋**: `a6fc251` (Phase 1 §6.3 — FieldGenerator)
